import React, { useEffect } from 'react';

const ListenForAccept = () => {
    //if user.role is donor then listen for accept from needy
  useEffect(() => {
    
    //create a chat room using the same random number id that was created in the needy page
    //give the name as needy name and profile
  }, []);

  return null;
};

export default ListenForAccept;
